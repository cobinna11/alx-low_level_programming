#include <stdio.h>

/**
 * main - the entry point for my program
 *
 * Description: This is the the one that runs everything
 * Return: The value that returns 0 for code success
 */

int main (void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
